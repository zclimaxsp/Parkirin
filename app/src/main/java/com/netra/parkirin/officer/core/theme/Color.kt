package com.netra.parkirin.officer.core.theme

import androidx.compose.ui.graphics.Color

// ── Primary (Teal/Cyan — parking/city government feel) ──
val Primary = Color(0xFF0D6E6E)          // Deep Teal
val PrimaryLight = Color(0xFF14B8A6)     // Teal 500
val PrimaryContainer = Color(0xFFCCFBF1) // Teal 100
val OnPrimaryContainer = Color(0xFF042F2E)

// ── Secondary (Amber — warm accent) ──
val Secondary = Color(0xFFD97706)        // Amber 600
val SecondaryLight = Color(0xFFF59E0B)   // Amber 400
val SecondaryContainer = Color(0xFFFEF3C7)
val OnSecondaryContainer = Color(0xFF451A03)

// ── Status Colors ──
val StatusSuccess = Color(0xFF059669)    // Emerald 600
val StatusWarning = Color(0xFFF59E0B)    // Amber 400
val StatusError = Color(0xFFDC2626)      // Red 600
val StatusInfo = Color(0xFF2563EB)       // Blue 600

// ── Neutral Surfaces ──
val Background = Color(0xFFF8FAFC)       // Slate 50
val Surface = Color(0xFFFFFFFF)
val SurfaceVariant = Color(0xFFF1F5F9)   // Slate 100
val SurfaceContainer = Color(0xFFE2E8F0) // Slate 200
val OnSurface = Color(0xFF0F172A)        // Slate 900
val OnSurfaceVariant = Color(0xFF64748B) // Slate 500

// ── GPS Status Colors ──
val GpsReady = Color(0xFF059669)
val GpsAcquiring = Color(0xFFF59E0B)
val GpsFailed = Color(0xFFDC2626)
val GpsBlocked = Color(0xFF6B7280)

// ── Dark Theme ──
val PrimaryDark = Color(0xFF14B8A6)
val BackgroundDark = Color(0xFF0B1120)
val SurfaceDark = Color(0xFF1E293B)
val SurfaceVariantDark = Color(0xFF334155)
val OnSurfaceDark = Color(0xFFF1F5F9)
val OnSurfaceVariantDark = Color(0xFF94A3B8)
